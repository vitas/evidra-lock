# kubectl-basic

Minimal declarative `kubectl` tool surface for ops profile.

Supported operations:
- `get`
- `apply`
- `delete`

All operations are explicit and schema-validated. No free-form shell arguments are allowed.

