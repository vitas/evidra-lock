# curl-basic Pack

This pack registers a controlled `curl` tool with operations:
- `version`
- `get`
- `post`

Load locally by setting:

```bash
EVIDRA_PACKS_DIR=./packs
```

Use the pack policy examples under `policy/` to govern allow/risk/reason outcomes.
